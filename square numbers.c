//write a function which will accepect two input parameters x,y and returns x^2 and y^2
#include <stdio.h>
int  sqar (int x , int y)
{
   int sqar = (x*x) + (y*y);
   return sqar ;

}
void main ()
{
    int x = 12;
    int y = 15;
    print ("%d",int )
}