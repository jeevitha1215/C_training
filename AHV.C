#include <stdio.h>
void main()
{
    int age;
    float h;
    printf("enter age");
    scanf("%d",&age);
    printf("enter heigth");
    scanf("%f",&h);
    if((age.12) && (h>4.5)) // 13>12 ? T AND 5>4.5?==> T
{
        printf("you can ride the roller coaster!");

}
    else
{
        printf("grow up and come back!");
}
}